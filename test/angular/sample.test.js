/**
 * Sample test
 */
describe("Sample test to ensure karma config works", () => {
  it('should pass', () => {
    expect(1).toEqual(1);
  });
  it('should fail', () => {
    expect(2).toEqual(1);
  });
});